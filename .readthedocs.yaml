version: 2

sphinx:
  # Path to your Sphinx configuration file.
  configuration: doc/conf.py

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

python:
  # version: 3.8
  install:
    - requirements: doc/requirements.txt
